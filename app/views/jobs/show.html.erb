<h1> <%= @job.title %> </h1>

<p>
  <%= simple_format(@job.description) %>

</p>
<div class="text-center">
  <%= link_to("点击报名，加入社群", new_job_resume_path(@job), :style => "font-size: 10px; text-decoration:underline;") %>
</div>
